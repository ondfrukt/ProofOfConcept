#include <Arduino.h>
#include "config.h"

void hookChange(int line, HookState newHookState ){

  if (newHookState == HOOK_OFF && lineSystem.getCurrentLineStatus(line) == line_idle){
    lineSystem.setLineStatus(line, statuses new_status)

  }


};
